# Staff Operations

The `staff_ops` app will hold employee, moderator, queue, and permission policy concepts. Phase 0 only introduces a lightweight authorization abstraction so later workflow code does not hard-code permission logic inside views or services.

