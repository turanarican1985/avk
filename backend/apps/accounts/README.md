# Accounts

The `accounts` app owns the project's custom user model. It is introduced early so future institution ownership, operators, staff users, moderators, and admins can all depend on a stable user reference without a risky late-stage auth migration.

