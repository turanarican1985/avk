# Audit Core

The `audit_core` app exists to keep auditability visible from the beginning. Phase 0 provides abstractions for recording events and for logging access to sensitive data without committing to the final storage model yet.

