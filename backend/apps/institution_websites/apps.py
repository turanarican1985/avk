from django.apps import AppConfig


class InstitutionWebsitesConfig(AppConfig):
    """Configuration for institution website concerns."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.institution_websites"
    verbose_name = "Institution Websites"
